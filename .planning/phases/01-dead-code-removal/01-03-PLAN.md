---
phase: 01-dead-code-removal
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/providers/captureCodeActionProvider.ts
  - src/extension.ts
  - src/constants.ts
  - package.json
autonomous: true
requirements: [CLEAN-03, CLEAN-05]

must_haves:
  truths:
    - "The Command Palette shows no commands related to smart capture insertion"
    - "TypeScript compilation produces zero errors after removing smart capture"
    - "package.json contains no contribution for posthog.insertCapture"
  artifacts:
    - path: "src/extension.ts"
      provides: "Extension activation with capture code action removed"
      contains_not: "CaptureCodeActionProvider|registerCaptureCommands|captureCodeActionProvider"
    - path: "src/constants.ts"
      provides: "Command constants with INSERT_CAPTURE removed"
      contains_not: "INSERT_CAPTURE"
    - path: "package.json"
      provides: "Extension manifest with no insertCapture command contribution"
      contains_not: "insertCapture"
  key_links:
    - from: "src/extension.ts"
      to: "src/constants.ts"
      via: "Commands import"
      pattern: "Commands\\.(SIGN_IN|SIGN_OUT|SELECT_PROJECT|REFRESH_FEATURE_FLAGS|COPY_FLAG_KEY|OPEN_FLAG_IN_BROWSER|CREATE_FLAG|SHOW_FLAG_DETAIL|SHOW_EXPERIMENT_DETAIL|SCAN_STALE_FLAGS|CLEANUP_STALE_FLAG|SHOW_SESSIONS)"
---

<objective>
Remove the smart capture insertion feature from the extension.

Purpose: Eliminate the dead smart capture feature (posthog.insertCapture code action) that is out of scope for v1. Per locked decision, this executes after HogQL editor (Plan 01) and error tracking (Plan 02) removal.

Output: Extension compiles cleanly with no trace of smart capture in source, manifest, or constants.
</objective>

<execution_context>
@/Users/fcgomes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fcgomes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dead-code-removal/01-CONTEXT.md
@.planning/phases/01-dead-code-removal/01-RESEARCH.md
@.planning/phases/01-dead-code-removal/01-01-SUMMARY.md
@.planning/phases/01-dead-code-removal/01-02-SUMMARY.md

<interfaces>
<!-- Key structures in files being modified. Executor needs these to make surgical edits. -->
<!-- NOTE: Line numbers are approximate -- read files first as Plans 01 and 02 will have shifted lines. -->

From src/constants.ts -- Commands object entry to REMOVE:
```typescript
INSERT_CAPTURE: 'posthog.insertCapture',
```

From src/extension.ts -- import to REMOVE:
```typescript
import { CaptureCodeActionProvider, registerCaptureCommands } from './providers/captureCodeActionProvider';
```

From src/extension.ts -- instantiation to REMOVE:
```typescript
const captureCodeActionProvider = new CaptureCodeActionProvider(treeSitter);
```

From src/extension.ts -- subscriptions to REMOVE from context.subscriptions.push():
```typescript
vscode.languages.registerCodeActionsProvider(languageSelector, captureCodeActionProvider, {
    providedCodeActionKinds: CaptureCodeActionProvider.providedCodeActionKinds,
}),
...registerCaptureCommands(),
```

From package.json contributes -- entry to REMOVE:
```json
{ "command": "posthog.insertCapture", "title": "PostHog: Track Function with PostHog" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove smart capture insertion feature</name>
  <files>src/providers/captureCodeActionProvider.ts, src/extension.ts, src/constants.ts, package.json</files>
  <read_first>
    - src/extension.ts (current state after Plans 01 and 02 modifications)
    - src/constants.ts (current state after Plans 01 and 02 modifications)
    - package.json (current state after Plans 01 and 02 modifications)
    - src/providers/captureCodeActionProvider.ts (confirm file exists before deleting)
  </read_first>
  <action>
    Delete source file:
    - Delete `src/providers/captureCodeActionProvider.ts`

    Edit `src/constants.ts`:
    - Remove this entry from the `Commands` object:
      ```
      INSERT_CAPTURE: 'posthog.insertCapture',
      ```

    Edit `src/extension.ts`:
    - Remove the import line: `import { CaptureCodeActionProvider, registerCaptureCommands } from './providers/captureCodeActionProvider';` (line number will have shifted after Plans 01 and 02 -- search for the string)
    - Remove the instantiation: `const captureCodeActionProvider = new CaptureCodeActionProvider(treeSitter);`
    - Remove these entries from `context.subscriptions.push(...)`:
      - The `vscode.languages.registerCodeActionsProvider(languageSelector, captureCodeActionProvider, { providedCodeActionKinds: CaptureCodeActionProvider.providedCodeActionKinds, }),` block
      - `...registerCaptureCommands(),`

    IMPORTANT: After removing entries from subscriptions.push(), verify comma placement is correct.

    Edit `package.json`:
    - Remove from `contributes.commands` array: the object with `"command": "posthog.insertCapture"`

    Run `pnpm compile` to verify zero TypeScript errors.
  </action>
  <verify>
    <automated>cd /Users/fcgomes/codehog && pnpm compile && ! grep -qE 'CaptureCodeActionProvider|registerCaptureCommands|captureCodeActionProvider|INSERT_CAPTURE|insertCapture' src/extension.ts src/constants.ts package.json && ! test -f src/providers/captureCodeActionProvider.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/providers/captureCodeActionProvider.ts` does not exist
    - `src/constants.ts` does NOT contain `INSERT_CAPTURE`
    - `src/extension.ts` does NOT contain `CaptureCodeActionProvider` or `registerCaptureCommands` or `captureCodeActionProvider`
    - `package.json` does NOT contain `insertCapture`
    - `pnpm compile` exits with code 0
  </acceptance_criteria>
  <done>Smart capture insertion feature is fully removed from source files, extension wiring, constants, and package.json contributions. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After task completes:
1. `pnpm compile` exits 0
2. `grep -rE 'captureCodeAction|registerCaptureCommands|insertCapture|INSERT_CAPTURE' src/ package.json` returns no matches
3. File deleted: `src/providers/captureCodeActionProvider.ts`
</verification>

<success_criteria>
- TypeScript compilation produces zero errors
- No smart capture commands exist in package.json contributes
- No smart capture imports, instantiations, or registrations exist in extension.ts
- No smart capture command IDs exist in constants.ts
- Source file for smart capture is deleted
</success_criteria>

<output>
After completion, create `.planning/phases/01-dead-code-removal/01-03-SUMMARY.md`
</output>
