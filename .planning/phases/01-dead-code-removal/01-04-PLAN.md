---
phase: 01-dead-code-removal
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - wasm/tree-sitter-python.wasm
  - wasm/tree-sitter-go.wasm
  - wasm/tree-sitter-ruby.wasm
  - grammars/tree-sitter-python.wasm
  - grammars/tree-sitter-go.wasm
  - grammars/tree-sitter-ruby.wasm
  - src/services/treeSitterService.ts
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "The wasm/ directory contains only JavaScript, TypeScript, TSX grammars and the tree-sitter runtime"
    - "The grammars/ directory contains only JavaScript, TypeScript, TSX grammar files"
    - "treeSitterService.ts has no Python, Go, or Ruby language definitions"
    - "TypeScript compilation produces zero errors"
    - "The .vsix bundle is measurably smaller (3-6 MB from removed WASM files)"
  artifacts:
    - path: "src/services/treeSitterService.ts"
      provides: "Tree-sitter service with only JS/TS/TSX language support"
      contains_not: "PYTHON_QUERIES|GO_QUERIES|RUBY_QUERIES|PY_CAPTURE|PY_FLAG|PY_ALL|GO_CAPTURE|GO_FLAG|GO_ALL|RB_CAPTURE|RB_FLAG|RB_ALL|python.*wasm|go.*wasm|ruby.*wasm"
    - path: "wasm/"
      provides: "WASM directory with only JS/TS/TSX grammars"
      contains_not: "tree-sitter-python.wasm|tree-sitter-go.wasm|tree-sitter-ruby.wasm"
    - path: "grammars/"
      provides: "Grammar source directory with only JS/TS/TSX grammars"
      contains_not: "tree-sitter-python.wasm|tree-sitter-go.wasm|tree-sitter-ruby.wasm"
  key_links:
    - from: "src/services/treeSitterService.ts"
      to: "wasm/"
      via: "LANG_FAMILIES map wasm property"
      pattern: "tree-sitter-(javascript|typescript|tsx)\\.wasm"
    - from: "webpack.config.js"
      to: "grammars/"
      via: "CopyPlugin pattern"
      pattern: "grammars/\\*\\.wasm"
---

<objective>
Remove Python, Go, and Ruby tree-sitter grammar support from the extension, keeping only JavaScript/TypeScript/TSX.

Purpose: Multi-language support is out of scope for v1. The removed WASM files account for 3-6 MB of bundle size reduction, which is the single biggest size win in this phase. Per locked decision, this executes last after all other dead code removals.

Output: The grammars/ and wasm/ directories contain only JS/TS/TSX files. treeSitterService.ts has only JS/TS/TSX language definitions. Extension compiles cleanly.
</objective>

<execution_context>
@/Users/fcgomes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fcgomes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dead-code-removal/01-CONTEXT.md
@.planning/phases/01-dead-code-removal/01-RESEARCH.md

<interfaces>
<!-- Key structures in treeSitterService.ts that the executor must understand to make surgical edits. -->

From src/services/treeSitterService.ts -- LANG_FAMILIES map (the single source of truth for language support):
```typescript
const LANG_FAMILIES: Record<string, LangFamily> = {
    javascript: { wasm: 'tree-sitter-javascript.wasm', ... },     // KEEP
    javascriptreact: { wasm: 'tree-sitter-javascript.wasm', ... }, // KEEP (shares JS grammar)
    typescript: { wasm: 'tree-sitter-typescript.wasm', ... },      // KEEP
    typescriptreact: { wasm: 'tree-sitter-tsx.wasm', ... },        // KEEP
    python: { wasm: 'tree-sitter-python.wasm', ... },              // DELETE
    go: { wasm: 'tree-sitter-go.wasm', ... },                     // DELETE
    ruby: { wasm: 'tree-sitter-ruby.wasm', ... },                 // DELETE
};
```

From src/services/treeSitterService.ts -- method sets to DELETE (approx lines 60-78):
```typescript
const PY_CAPTURE_METHODS = new Set(['capture']);
const PY_FLAG_METHODS = new Set([...]);
const PY_ALL_METHODS = new Set([...]);
const GO_CAPTURE_METHODS = new Set(['Capture']);
const GO_FLAG_METHODS = new Set([...]);
const GO_ALL_METHODS = new Set([...]);
const RB_CAPTURE_METHODS = new Set(['capture']);
const RB_FLAG_METHODS = new Set([...]);
const RB_ALL_METHODS = new Set([...]);
```

From src/services/treeSitterService.ts -- query objects to DELETE (approx lines 194-329):
```typescript
const PYTHON_QUERIES: QueryStrings = { ... };   // entire object
const GO_QUERIES: QueryStrings = { ... };        // entire object
const RUBY_QUERIES: QueryStrings = { ... };      // entire object
```

From wasm/ directory -- KEEP these files:
- tree-sitter.wasm (runtime)
- tree-sitter-javascript.wasm
- tree-sitter-typescript.wasm
- tree-sitter-tsx.wasm

From grammars/ directory -- KEEP these files:
- tree-sitter-javascript.wasm
- tree-sitter-typescript.wasm
- tree-sitter-tsx.wasm

From webpack.config.js -- the CopyPlugin pattern `grammars/*.wasm` copies ALL .wasm files from grammars/ to wasm/. Deleting from grammars/ prevents them from being re-copied on next build. The pattern itself does NOT need to change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Python, Go, Ruby WASM files from wasm/ and grammars/</name>
  <files>wasm/tree-sitter-python.wasm, wasm/tree-sitter-go.wasm, wasm/tree-sitter-ruby.wasm, grammars/tree-sitter-python.wasm, grammars/tree-sitter-go.wasm, grammars/tree-sitter-ruby.wasm</files>
  <read_first>
    - List contents of wasm/ directory (ls wasm/) to confirm all 3 files exist
    - List contents of grammars/ directory (ls grammars/) to confirm all 3 files exist
    - webpack.config.js (confirm CopyPlugin copies from grammars/*.wasm -- both source locations must be cleaned)
  </read_first>
  <action>
    Delete these 6 files:
    - `wasm/tree-sitter-python.wasm`
    - `wasm/tree-sitter-go.wasm`
    - `wasm/tree-sitter-ruby.wasm`
    - `grammars/tree-sitter-python.wasm`
    - `grammars/tree-sitter-go.wasm`
    - `grammars/tree-sitter-ruby.wasm`

    After deletion, verify remaining files:
    - `wasm/` should contain: `tree-sitter.wasm`, `tree-sitter-javascript.wasm`, `tree-sitter-typescript.wasm`, `tree-sitter-tsx.wasm` (4 files)
    - `grammars/` should contain: `tree-sitter-javascript.wasm`, `tree-sitter-typescript.wasm`, `tree-sitter-tsx.wasm` (3 files)

    IMPORTANT: Both locations must be cleaned. The `grammars/` directory is the source for webpack's CopyPlugin. If you only delete from `wasm/`, the next `pnpm compile` will re-copy the grammars back via the CopyPlugin rule `grammars/*.wasm -> wasm/[name][ext]`.

    Do NOT modify `webpack.config.js` -- the glob pattern `grammars/*.wasm` is fine because it will now only match the 3 remaining JS/TS/TSX grammars.
  </action>
  <verify>
    <automated>cd /Users/fcgomes/codehog && ls wasm/ | sort && echo "---" && ls grammars/ | sort && echo "---" && ! test -f wasm/tree-sitter-python.wasm && ! test -f wasm/tree-sitter-go.wasm && ! test -f wasm/tree-sitter-ruby.wasm && ! test -f grammars/tree-sitter-python.wasm && ! test -f grammars/tree-sitter-go.wasm && ! test -f grammars/tree-sitter-ruby.wasm && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `wasm/` contains exactly 4 files: tree-sitter.wasm, tree-sitter-javascript.wasm, tree-sitter-typescript.wasm, tree-sitter-tsx.wasm
    - `grammars/` contains exactly 3 files: tree-sitter-javascript.wasm, tree-sitter-typescript.wasm, tree-sitter-tsx.wasm
    - None of the 6 deleted files exist
  </acceptance_criteria>
  <done>Python, Go, and Ruby WASM grammar files are deleted from both wasm/ and grammars/ directories. Only JS/TS/TSX grammars remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Python, Go, Ruby language definitions from treeSitterService.ts</name>
  <files>src/services/treeSitterService.ts</files>
  <read_first>
    - src/services/treeSitterService.ts (full file -- identify exact line ranges for: PY_CAPTURE_METHODS, PY_FLAG_METHODS, PY_ALL_METHODS, GO_CAPTURE_METHODS, GO_FLAG_METHODS, GO_ALL_METHODS, RB_CAPTURE_METHODS, RB_FLAG_METHODS, RB_ALL_METHODS, PYTHON_QUERIES, GO_QUERIES, RUBY_QUERIES, and python/go/ruby entries in LANG_FAMILIES)
  </read_first>
  <action>
    Edit `src/services/treeSitterService.ts`:

    1. Remove all Python method set constants:
       - `const PY_CAPTURE_METHODS = new Set([...]);`
       - `const PY_FLAG_METHODS = new Set([...]);`
       - `const PY_ALL_METHODS = new Set([...]);`

    2. Remove all Go method set constants:
       - `const GO_CAPTURE_METHODS = new Set([...]);`
       - `const GO_FLAG_METHODS = new Set([...]);`
       - `const GO_ALL_METHODS = new Set([...]);`

    3. Remove all Ruby method set constants:
       - `const RB_CAPTURE_METHODS = new Set([...]);`
       - `const RB_FLAG_METHODS = new Set([...]);`
       - `const RB_ALL_METHODS = new Set([...]);`

    4. Remove the three query objects:
       - `const PYTHON_QUERIES: QueryStrings = { ... };` (entire object, may span many lines)
       - `const GO_QUERIES: QueryStrings = { ... };` (entire object)
       - `const RUBY_QUERIES: QueryStrings = { ... };` (entire object)

    5. Remove from LANG_FAMILIES map:
       - The `python: { ... },` entry (entire object including all properties)
       - The `go: { ... },` entry
       - The `ruby: { ... },` entry
       - After removal, LANG_FAMILIES should contain ONLY: javascript, javascriptreact, typescript, typescriptreact

    6. Check for any references to removed constants that might remain:
       - Search for `PY_`, `GO_`, `RB_`, `PYTHON_QUERIES`, `GO_QUERIES`, `RUBY_QUERIES`, `python`, `go`, `ruby` in the file
       - Remove any remaining references (these should all be inside the LANG_FAMILIES entries being deleted, so step 5 should cover them)

    Run `pnpm compile` to verify zero TypeScript errors.

    The `supportedLanguages` getter and `isSupported()` method derive from the LANG_FAMILIES map dynamically -- they require NO changes. Removing entries from the map automatically removes those languages from support.
  </action>
  <verify>
    <automated>cd /Users/fcgomes/codehog && pnpm compile && ! grep -qE 'PY_CAPTURE|PY_FLAG|PY_ALL|GO_CAPTURE|GO_FLAG|GO_ALL|RB_CAPTURE|RB_FLAG|RB_ALL|PYTHON_QUERIES|GO_QUERIES|RUBY_QUERIES' src/services/treeSitterService.ts && ! grep -qP "^\s+(python|go|ruby)\s*:" src/services/treeSitterService.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm compile` exits with code 0
    - `src/services/treeSitterService.ts` does NOT contain `PY_CAPTURE_METHODS`, `PY_FLAG_METHODS`, `PY_ALL_METHODS`
    - `src/services/treeSitterService.ts` does NOT contain `GO_CAPTURE_METHODS`, `GO_FLAG_METHODS`, `GO_ALL_METHODS`
    - `src/services/treeSitterService.ts` does NOT contain `RB_CAPTURE_METHODS`, `RB_FLAG_METHODS`, `RB_ALL_METHODS`
    - `src/services/treeSitterService.ts` does NOT contain `PYTHON_QUERIES`, `GO_QUERIES`, or `RUBY_QUERIES`
    - `src/services/treeSitterService.ts` LANG_FAMILIES map contains only keys: `javascript`, `javascriptreact`, `typescript`, `typescriptreact`
    - `grep -c 'wasm' src/services/treeSitterService.ts` shows references only to javascript, typescript, and tsx WASM files
  </acceptance_criteria>
  <done>treeSitterService.ts contains only JS/TS/TSX language definitions. All Python, Go, and Ruby method sets, query objects, and LANG_FAMILIES entries are removed. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm compile` exits 0
2. `ls wasm/` shows exactly: tree-sitter.wasm, tree-sitter-javascript.wasm, tree-sitter-typescript.wasm, tree-sitter-tsx.wasm
3. `ls grammars/` shows exactly: tree-sitter-javascript.wasm, tree-sitter-typescript.wasm, tree-sitter-tsx.wasm
4. `grep -cE 'python|ruby|go' src/services/treeSitterService.ts` returns 0 (or only in comments)
5. Run `pnpm compile` again to confirm webpack CopyPlugin does NOT re-create deleted WASM files
6. Measure bundle size: `du -sh wasm/` should be measurably smaller than before (expect 3-6 MB reduction)
</verification>

<success_criteria>
- TypeScript compilation produces zero errors
- No Python, Go, or Ruby WASM files exist in wasm/ or grammars/
- treeSitterService.ts has no Python, Go, or Ruby language configurations
- Extension supports only JavaScript, TypeScript, JSX, and TSX
- Bundle size is reduced by the size of 3 WASM grammars
</success_criteria>

<output>
After completion, create `.planning/phases/01-dead-code-removal/01-04-SUMMARY.md`
</output>
