---
phase: 05-analytics-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/webview/styles.ts
  - src/views/webview/layout.ts
  - src/views/webview/script.ts
autonomous: true
requirements: [ANLY-04]
must_haves:
  truths:
    - "While insights are loading, the Analytics tab shows 3 animated skeleton placeholder cards"
    - "Skeleton cards match the dimensions of real insight-card elements"
    - "Skeleton cards disappear when real data renders"
  artifacts:
    - path: "src/views/webview/styles.ts"
      provides: "Skeleton card CSS with shimmer animation"
      contains: "skeleton-card"
    - path: "src/views/webview/layout.ts"
      provides: "Skeleton HTML replacing 'Loading insights...' text"
      contains: "skeleton-card"
    - path: "src/views/webview/script.ts"
      provides: "renderInsights hides skeleton container"
      contains: "skeleton"
  key_links:
    - from: "src/views/webview/script.ts"
      to: "layout.ts skeleton HTML"
      via: "renderInsights hides analytics-loading element"
      pattern: "analytics-loading.*display.*none"
---

<objective>
Add animated skeleton loading placeholder cards to the Analytics tab.

Purpose: Replace the plain "Loading insights..." text with 3 shimmer-animated skeleton cards that preview the insight-card layout, giving users immediate visual feedback while insights load from the PostHog API.

Output: Updated styles.ts, layout.ts, and script.ts with skeleton loading state.
</objective>

<execution_context>
@/Users/fcgomes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fcgomes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton card CSS and shimmer animation</name>
  <files>src/views/webview/styles.ts</files>
  <read_first>src/views/webview/styles.ts (lines 721-775 for .insight-card dimensions)</read_first>
  <action>
Add the following CSS classes to styles.ts, placed immediately after the existing `/* -- Insight cards -- */` section (after the `.insight-card-empty` rule around line 780):

1. `.skeleton-card` - matches `.insight-card` dimensions: same `background`, `border`, `border-radius: 6px`, `padding: 12px`, but with `cursor: default` (not pointer), no hover state. Add `overflow: hidden` and `position: relative`.

2. `.skeleton-card-header` - matches `.insight-card-header`: `display: flex`, `align-items: center`, `gap: 6px`, `margin-bottom: 8px`.

3. `.skeleton-bone` - the individual shimmer element: `background: var(--vscode-input-background, rgba(255,255,255,0.08))`, `border-radius: 4px`, `position: relative`, `overflow: hidden`.

4. `.skeleton-bone::after` - the shimmer sweep pseudo-element:
   ```
   content: '';
   position: absolute;
   top: 0; left: 0; right: 0; bottom: 0;
   background: linear-gradient(90deg, transparent, rgba(255,255,255,0.04), transparent);
   animation: shimmer 1.5s infinite;
   ```

5. `@keyframes shimmer` - `0% { transform: translateX(-100%); }` `100% { transform: translateX(100%); }`

6. Dimension variants for the bones:
   - `.skeleton-bone.icon` - `width: 20px; height: 20px; border-radius: 4px; flex-shrink: 0;`
   - `.skeleton-bone.title` - `height: 12px; flex: 1;`
   - `.skeleton-bone.type` - `height: 10px; width: 40px; flex-shrink: 0;`
   - `.skeleton-bone.chart` - `height: 44px; width: 100%;` (matches .insight-card-body min-height)

7. Light theme support: Add a rule `.vscode-light .skeleton-bone::after` that uses `rgba(0,0,0,0.04)` instead of `rgba(255,255,255,0.04)` for the shimmer gradient. Also `.vscode-light .skeleton-bone` uses `background: rgba(0,0,0,0.06)`.
  </action>
  <verify>
    <automated>cd /Users/fcgomes/codehog && pnpm compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "skeleton-card" src/views/webview/styles.ts
    - grep -q "skeleton-bone" src/views/webview/styles.ts
    - grep -q "@keyframes shimmer" src/views/webview/styles.ts
    - grep -q "vscode-light.*skeleton" src/views/webview/styles.ts
  </acceptance_criteria>
  <done>styles.ts contains skeleton-card, skeleton-bone, shimmer keyframes, and light theme variant</done>
</task>

<task type="auto">
  <name>Task 2: Replace loading text with skeleton cards in layout and wire visibility in script</name>
  <files>src/views/webview/layout.ts, src/views/webview/script.ts</files>
  <read_first>src/views/webview/layout.ts (lines 80-88 for analytics section), src/views/webview/script.ts (lines 221-250 for renderInsights function)</read_first>
  <action>
**In layout.ts:**

Replace the current analytics loading div (line ~82):
```html
<div class="loading" id="analytics-loading">Loading insights...</div>
```

With 3 skeleton placeholder cards inside the same container (keep the same `id="analytics-loading"` so script.ts hide logic still works):
```html
<div class="insight-grid" id="analytics-loading">
  <div class="skeleton-card">
    <div class="skeleton-card-header">
      <div class="skeleton-bone icon"></div>
      <div class="skeleton-bone title"></div>
      <div class="skeleton-bone type"></div>
    </div>
    <div class="skeleton-bone chart"></div>
  </div>
  <div class="skeleton-card">
    <div class="skeleton-card-header">
      <div class="skeleton-bone icon"></div>
      <div class="skeleton-bone title"></div>
      <div class="skeleton-bone type"></div>
    </div>
    <div class="skeleton-bone chart"></div>
  </div>
  <div class="skeleton-card">
    <div class="skeleton-card-header">
      <div class="skeleton-bone icon"></div>
      <div class="skeleton-bone title"></div>
      <div class="skeleton-bone type"></div>
    </div>
    <div class="skeleton-bone chart"></div>
  </div>
</div>
```

Note: Use `class="insight-grid"` on the skeleton container so it uses the same flex column layout and gap as the real grid. Keep the `id="analytics-loading"` so the existing `renderInsights()` function in script.ts hides it with `loading.style.display = 'none'`.

**In script.ts:**

In the `renderInsights` function (around line 223), the existing code already does:
```js
const loading = document.getElementById('analytics-loading');
loading.style.display = 'none';
```

This already handles hiding the skeleton. No changes needed to the hide logic.

However, verify the error case also hides the skeleton. Search for the `analytics` error handler in script.ts. If there is a message handler for `type: 'error'` with `section: 'analytics'`, ensure it also sets `analytics-loading` display to none. If not already handled, add: `document.getElementById('analytics-loading').style.display = 'none';` in that error path.
  </action>
  <verify>
    <automated>cd /Users/fcgomes/codehog && pnpm compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "skeleton-card" src/views/webview/layout.ts
    - grep -q "skeleton-bone" src/views/webview/layout.ts
    - grep -c "skeleton-card" src/views/webview/layout.ts returns 3 or more
    - grep -q "analytics-loading" src/views/webview/layout.ts
  </acceptance_criteria>
  <done>Analytics tab shows 3 skeleton cards with shimmer animation while loading, and skeletons disappear when insights arrive or on error</done>
</task>

</tasks>

<verification>
- `pnpm compile` succeeds with zero errors
- layout.ts contains 3 skeleton-card elements inside analytics-loading container
- styles.ts contains skeleton-card, skeleton-bone, shimmer animation, and light theme variant
- The analytics-loading element retains its ID so renderInsights can hide it
</verification>

<success_criteria>
- 3 animated skeleton cards render in the Analytics tab while insights are loading
- Skeleton cards match insight-card layout (icon, title, type badge, chart area)
- Skeletons disappear when real data renders
- Shimmer animation works in both light and dark VS Code themes
</success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-tab/05-01-SUMMARY.md`
</output>
